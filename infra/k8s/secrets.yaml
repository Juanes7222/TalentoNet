apiVersion: v1
kind: Secret
metadata:
  name: talentonet-secrets
  namespace: talentonet
type: Opaque
stringData:
  # NOTA: En producción usar Sealed Secrets o External Secrets Operator
  # Estos valores son placeholders, deben reemplazarse con valores reales
  db-host: "postgres-service.talentonet.svc.cluster.local"
  db-username: "talentonet"
  db-password: "CHANGE_ME_PRODUCTION_PASSWORD"
  jwt-secret: "CHANGE_ME_PRODUCTION_JWT_SECRET_MINIMUM_32_CHARS"
  jwt-refresh-secret: "CHANGE_ME_PRODUCTION_REFRESH_SECRET"
  rabbitmq-url: "amqp://user:password@rabbitmq-service.talentonet.svc.cluster.local:5672"
  aws-access-key: "CHANGE_ME_AWS_ACCESS_KEY"
  aws-secret-key: "CHANGE_ME_AWS_SECRET_KEY"
  yeminus-api-key: "CHANGE_ME_YEMINUS_API_KEY"
